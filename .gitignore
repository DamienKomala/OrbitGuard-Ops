node_modules/
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

.DS_Store
.env*.local
.env*
.vercel

# Machine-local Claude Code settings (personal, not shared)
.claude/settings.local.json

# Per-agent skill copies generated by `npx skills`. The canonical install lives
# in .agents/skills/ and is pinned by skills-lock.json; these are 2MB duplicates
# for other agents and are reproducible with `npx skills experimental_install`.
/agent/
/.cursor/
/.github/copilot-instructions.md
